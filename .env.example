# Copy to .env and fill in. The app works without a key (uses heuristic fallback),
# but real LLM responses require an OpenAI key.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
FLASK_SECRET_KEY=change-me-in-prod
